# 099—139返片与开场接入

用户2026-09-20提供41段返片，授权接入、配音、检测后提交，并通过牛马小助手通知。原始MP4保存在 `art/video-pipeline/completion-v5/returned/`，逐文件SHA256见 sources.json。返片实际24fps、97或121帧，按实际时长播放，不按文件名建议时间强行加速。

## 行为变化

|素材|接入路径|
|---|---|
|99/100|蜷睡21醒到侧坐后直接朝左右站起，再移动；取消18→正面坐姿的绕路|
|101—104|左右站立与趴卧直接衔接；吃喝离开盆后到安全空位直接趴下|
|105—107|侧躺、露肚、舒展直接朝左起身；原右向起身保留|
|108—118|独立跑步起步/循环/停步、转正面四足、蹭头、三连叫、坐定，组成完整开场|
|119/120|左右站姿致谢；带路示意补给后完整结束当前循环，再站姿致谢，消除为致谢先坐下的绕路|
|121—124|蜷睡与趴卧、侧躺互通；加入地面睡姿与换姿权重|
|125—139|五种躺姿各自的提起、悬空循环和放回；提前松手仍完成合法落地并回原姿态|

每次真正创建桌宠主进程时触发一次；旧存档、普通重开和更新后的重新启动同样触发。同一进程内不重复触发，单实例拦截的重复启动不会创建第二次开场。检测到 Wallpaper Engine 时临时使用兼容悬浮层，避免桌面渲染层遮挡，不改用户保存的悬浮设置。工作台双击开场为手动预览。全屏/主动隐藏暂停表演时间，照料有效运行时间仍累计。点击/拖拽/手动命令沿姿态过渡取消。原音量、静音、自启开关和家具位置保留。

工作台64项参数；新增窝内等待、踱步范围、往返次数，以及蜷睡的进入和换姿权重。默认等待2秒、范围100、往返1次。真实跑步脚底速度约右137.5、左129.6逻辑单位/秒；短路程保留完整起停，以位移拟合路程。

## 三连叫与配音

117返片仅两次可见张嘴，源帧12—32、61—80。已向用户说明，并在可选确认未回复后按公开说明的推荐实现复用第一次完整开合嘴。运行时引用源帧 `0..47 + 0..47 + 48..96`，原视频及97张PNG保留，共145帧约6.04秒。三声分别绑定实际绘制帧12、60、109；闭嘴33、81、129停声。不是生成新口型，也不把三段音频挤进两次张嘴。用户若选择保留原片两声，应回退该编排。

使用用户的叫声1/3/4，原7个MP3字节不变；运行WAV去除前置空白、保持音高并匹配张嘴窗口。现有9个动作配音保留，119/120没有可见张嘴，不强配。92仍因背向镜头嘴部遮挡待校准。

## 几何与检测

统一768画布留足提起、伸肢边界，物理像素比例不变。SIFT共同实体特征配准首尾，不逐帧按外框缩放。82个端点配准均接受，41段无画布硬裁边。跑步、悬空和蹭头使用固定根锚点，保留原片腾空/身体运动。105/107/122/123/124使用人工核对承重脚或胸腹的支撑关键点，排除尾巴和悬空肢体。639帧清除了与透明边缘连通的浅色地面残片；原片不改。

证据在 `docs/qa/completion-v5/`；完整WPF导出、逐对报告、前后帧和叠图保留于 `artifacts/completion-v5-integration/`。机器检测的姿态变化、低置信度、源片形变警报不隐瞒、不因素材齐全而清零。原生音频验证涵盖PCM预载与绘制帧事件，不代表已测扬声器硬件延迟。

## 重建

依次运行 `tools/install_completion_videos.py`、`tools/refine_completion_matte.py`、`tools/calibrate_completion_videos.py`、`tools/compose_startup_calls.py`、`tools/prepare_cat_voice.py`。原生 `--audit-completion` 导出左右开场、五姿态提放与全部片段；`tools/check_animation_continuity.py` 扫描逐帧/循环/共享端点；`tools/render_startup_review.py` 用真实绘制事件合成有声预览。`tools/update_completion_delivery.py` 更新制作参考索引，原首尾图与提示词继续保留。

剩余精修：真正三次张嘴的117可替换当前复用编排；原86步态相位、62/63翻身与74形体属于旧返片精修；92的可见嘴型标记仍缺。41项动作本身已齐，不再作为缺片要求用户重做。

## 本轮结果与未清零项

- 750项逻辑检查、13项Windows检查通过；工作台单击/双击、保存读回、非法值拒绝、开场预览与64项参数通过原生窗口验证。
- 原生左窝开场1730帧/72.08秒，右窝1590帧/66.25秒，均在中部坐定，库存/照料期限未变；三次实际绘制叫声事件分别落在117的12/60/109帧。
- 五种提放原生序列共1759帧，高度0—24，最大相邻变化0.375逻辑单位；起提/放回端点不跳变。猫窝82个场景样本旧错误负对照遮挡3140像素，新前沿上半身遮挡0。
- 全量6727帧、6851对比较：严格0.8%仿射尺寸阈值有1191处警报，其中1185处片内、6处旧片接缝；另1333处轮廓、位移或低置信度复核候选，两类可重叠。新片参与的共享接缝、全部循环均无高置信度尺寸警报。
- 新片片内916处警报，运行元数据相邻宽度变化最大仅0.00116%；已抽查最明显的106/105/100/107抬身与转身，警报包含姿态透视/局部躯体形变，不能等同整猫在运行时缩放，也没有把全部警报判为误报。原片进一步精修仍有必要；前后帧/叠图见 `qa/completion-v5/review-source-deformation.jpg`，原始警报保留在 `monitor-flags.json`。
- 6处旧片接缝：06→10、10→右走、08→12、09→01、15→12、19→20，估计宽/高差约0.9%—2.14%；本轮不擅自重制旧片。
- 4457张新PNG及处理记录、5个清单/音频文件、252个发布根文件与正式安装逐文件哈希核对一致。静音False、音量0.25、自启开关关闭与原摆放/行为配置保留；未强行替用户开启自启。

批量审计曾因WPF非托管图像累积失败，现按24帧释放检测图像并支持在输入像素/元数据未变时续导；最初错误日志保留本地，最终完整导出重新核对。监测器的跨片边必须使用**入边尾姿态**，不能把22低头结束帧当站姿或19睡下结束帧当侧坐；已更正并重新扫描全部接缝，片内结果复用同批未变像素的全量扫描。原生动作图像与元数据未因诊断阈值做外框归一化。
