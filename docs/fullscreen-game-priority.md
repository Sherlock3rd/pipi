# 阴影强度与全屏游戏优先（2026-09-19）

阴影透明度从0.19加倍到0.38；模糊半径14和向下偏移3逻辑单位保持，轮廓范围和动画不变。

旧全屏逻辑只检查前台窗口。现场bg3的SDL窗口为主屏0,0–1920,1080全屏，切副屏或自身窗口时不能用前台窗口不全屏证明本屏游戏已退出。新增目标屏Z序查询：直接前台全屏优先；前台为本屏普通应用时按原规则显示；焦点在另屏/自身/桌面时，查找本屏最上层有效外部应用。普通应用挡住游戏则恢复，忽略最小化、隐藏、cloaked、自身、桌面以及透明/不激活的工具层，保持另一屏全屏不误隐藏本屏。

陈皮宿主增加WS_EX_NOACTIVATE，点击不会抢走游戏前台；设置窗口仍独立。全屏避让时隐藏整个场景并临时取消Topmost，恢复后按保存的悬浮设置还原，不持久化临时状态。

验证：345项纯逻辑检查、13项Windows HWND检查通过。新增只读探针：

```powershell
.tools/dotnet/dotnet.exe run --project tests/Chenpi.Windows.Tests -c Release -- --probe-fullscreen
```

现场已得到主屏Hide=true，原因分别为fullscreen-on-pet-screen和fullscreen-visible-on-pet-screen；副屏Hide=false。不读取窗口标题，不激活/调整游戏或发送按键。原生夹具不代表对所有游戏、驱动和独占模式的验证。正式运行、阴影截图结果随后追加。

最终画面核对：38%阴影已在实际WPF截图确认，仍为柔和外轮廓投影。正式程序响应、诊断为启动后新写入，NOACTIVATE原生样式位已生效；此时主屏普通浏览器在前台，场景按规则可见，无新错误日志。现场游戏前台/副屏焦点探针与原生样式验证分开记录，不冒充游戏操作验收。量化记录docs/qa/shadow-fullscreen/verification.json。截图时游戏仍在运行，平均帧间隔39.26ms，保留该数据，不宣称独立性能基准通过。

安装与Git总账：阴影加倍及游戏优先实现2f90c834b9c0c68cb18a66b07f4fe8886d96ad17已推送origin/main并核验远端一致。345项逻辑和13项原生检查通过，当前bg3主屏全屏检测及副屏焦点补查符合预期；正式运行包已更新，存档已备份，DLL安装哈希一致，进程响应且无新错误日志。

