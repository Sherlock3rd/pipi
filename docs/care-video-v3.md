# 照料与互动返片接入

2026-09-18：按用户“就按我现在提供的进行替换，有问题后续再修”的授权，将 22—51 共 30 条返片直接用于运行时。原视频逐字节归档在 `art/video-pipeline/care-v3/returned`，每条记录 SHA256、实际帧数、帧率与原文件名。

保留原片全部 121 帧、24 fps、约 5.04 秒时长；不按先前制作提示的建议秒数裁剪，不重画、不镜像。仅沿用高清透明提取流程，生成 512×512 PNG，共 3630 张新增帧。逻辑显示尺寸与底部水平线规则保持现状。

动作对应：

- 吃饭 22→23→24，喝水 22→25→24；一次访问消耗一层，只在循环段计入进食进度。
- 如厕 26→27→28，刨砂 29→30→31。
- 拎起 32→33 循环，放下 34；右抓 29→35，结束收爪 31；左抓 36。
- 摸头 37、蹭鼠标 38、点击爪爪 39、卧躺 40→41→42。
- 左右带路回望 43/44；请求添饭/加水/铲屎 45/46/47；物品旁示意 48/49/50；照料后回应 51。

有限动作按返片实际时长完成，持续拖拽、抓棒及请求状态保持对应循环。直接互动可中断当前播放。动作查看器新增整组条目及吃喝、如厕、拎起、抓棒、卧躺组合路线，并可打开本次原片对照。

本轮验收针对替换完整性、播放路由、补给时机、可构建和运行。源动画的造型、端点姿势及动作表现按用户要求留待后续修订。

返片接入验收：176项模型/播放路由/补给时机检查通过；5883张运行帧及5个透明道具检查通过；全帧阈值审计孤立透明分量=0、轮廓近白像素=0。33悬空循环仅调整画布留白并同步锚点，以保留尾巴且不改变世界位置或显示大小。WPF实际预览确认22→23吃饭循环及32→33悬空循环，平均帧间隔约16.78ms/16.72ms；截图保留artifacts/care-v3-qa，不宣称逐条人工动态验收。Release自包含发布通过，正式包5883张帧SHA256与源一致、DLL一致，用户存档已备份before-care-v3。原动画表现按用户要求留待后续；准备提交推送origin/main。
