# 低盆沿、埋砂状态与短距离动作（2026-09-19）

用户再次提供穿模截图，要求降低食水盆、区分便便埋完与没埋完，并处理剩余距离不足以播完整动作的问题。

食水盆沿地面底座将竖向轮廓降为原来的72%，横向宽度不变。盆体、粮水内容、盆口前后层以及透明像素点击判定共用变换，世界地面线不下移。原图不改、猫不缩放。盆沿降低约9～12逻辑单位；整体物件顶端降低约17～18单位。

每个污物槽保存0～1的埋砂进度（只在内部存档使用，不显示数字）。如厕新增露出的深色污物，29抬手不增加覆盖，30实际刨砂逐渐覆盖浅色砂堆，31收爪保持覆盖结果。中断与读档保留未完成进度，埋完不扣猫砂脏污量；点击清理才移除该槽和进度。没有旧进度信息的存档按未埋表现，不凭空宣称埋完。

固定落点的移动先按正式动作图预测完整的起身、转向、起步和停步，并只加入完整行走循环。按路径长度分配根节点位移，短距离缩短空间步幅，不修改视频帧数/帧率，不越过目标后折返；到点等待停步结束，再开始吃喝等行为。目标改变时从当前帧和位置重新接续，提起仍可立即打断。仅埋砂抬手29继续使用已授权的3倍速度。

现有素材是固定幅度的栅格序列帧，缩短根节点位移不等于重画了脚步：极短距离仍可能有脚底滑动。若要1～8单位距离也完全匹配落脚，需要额外小步挪动素材或可变步幅骨骼；本次不把现有大步截帧或提速冒充小步动画。

验证：303项检查通过，包含左右各1/8/35/95/500单位完整移动、0及左右1/8/35单位完整食盆接近、重定目标、拖拽打断、埋砂中断与存档/清理。Release构建零警告零错误、自包含发布成功。真实WPF分别连续记录喝水35秒、吃饭24秒、如厕到埋砂离开60秒；吃喝均覆盖22→23/25→24，准备/循环/收尾各保持约5秒，猫砂记录0→部分→1覆盖且离开后仍保留砂堆。通过的三组预览均无error.log；首轮失败预览排除。量化记录docs/qa/contact-v3/validation.json，画面仅保留在忽略目录artifacts/contact-v3/。

最终补充：目标改到身后时，原方向的剩余动作及真实转身期间保持当前位置，进入新方向迈步片段后才位移；新增回归通过。此改动不涉及已核对的吃喝/猫砂画面和时长。

安装及Git总账：正式存档已备份before-contact-v3，等待精确路径旧进程退出后更新运行包；最终DLL/manifest与验证版本SHA256一致。新进程响应，诊断时间晚于启动时间，sceneVisibility=Visible、native.visible=true，无启动后的新错误日志，存档已保存5个覆盖槽。实现87bdf423430fa71f5e46508f61d86c9ff192a0ca已成功推送origin/main并核验远端一致；最终303项检查通过。
