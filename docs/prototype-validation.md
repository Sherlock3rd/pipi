# Windows 原型交付与验证

2026-09-17。用户已授权“先做起来”，随后要求重试、取消具体数值显示、紧凑布局以及自由拖动物件。

## 最新增量：随机照料与带路求助

2026-09-17 全屏范围补强：检测窗口可见性、最小化、DWM cloaked 状态与可见边界，按猫所在显示器的物理像素范围判断。避免副屏全屏误隐藏主屏猫；普通带标题栏最大化保持显示，含任务栏自动隐藏时的几何情形。调试 JSON 增加 `fullscreenReason` 和前台句柄，便于区分真正避让与窗口呈现故障。

- 110 项模型／策略检查通过；新增 13 项覆盖跨屏、负坐标、跨屏覆盖、普通最大化、隐藏／最小化／cloaked、自身／桌面／任务栏排除、两像素容差及进入退出序列。
- 新增 `tests/Chenpi.Windows.Tests`，本机连接两屏时 12 项 Windows 检查通过：真实临时 HWND 的窗口样式、可见边界、普通最大化、无边框铺屏、恢复尺寸、另屏铺屏、隐藏、最小化、已关闭句柄。测试窗口不主动激活；策略测试只替换读取结果的自身进程身份以模拟外部应用，其余来自 Win32/DWM。这不是前台切换或真实游戏独占全屏的端到端验收。
- Release 自包含发布成功；跨重启计时、真实游戏／视频／F11、混合 DPI 切换、Explorer 重启仍保留待验收状态。
- 隔离预览导出 `artifacts/test-fullscreen-policy/preview.png`，猫与六件场景元素正常绘制，无台词／判定圈。正式存档备份为 `.before-fullscreen-policy` 后更新并启动 `dist/Chenpi/Chenpi.exe --settings`，进程响应正常，领养日期、悬浮设置和猫窝位置保留，正式／打包 DLL 哈希一致。应用自身渲染图不作为桌面呈现或真实全屏验收证据。
- API 依据：[GetWindowRect 的边界及 DPI 说明](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-getwindowrect)、[DWM 可见边界和 cloaked 标志](https://learn.microsoft.com/en-us/windows/win32/api/dwmapi/ne-dwmapi-dwmwindowattribute)。实现与限制见[PROTOTYPE-005](../mistakes/prototype-005.md)。

2026-09-17 调试与入窝修复：新增用例先复现“引导期间点击调试喝水”失败，修复后共 97 项通过。覆盖五个调试动作整段执行、库存真实结算、请求恢复、15 秒安定期、点击立即唤醒、召回、拖拽中断和猫窝头部释放／边界。独立预览 `artifacts/test-manual-priority` 观测 `sleep`、窝内睡眠、请求与引导清空；UI 工具检测用户输入后停止操作，不夸大为全部按钮和实际拖放自动验收通过。详见[PROTOTYPE-004](../mistakes/prototype-004.md)。

2026-09-17 窗口修复追加：用户明确切换悬浮开关后消失。使用 `artifacts/test-layer-diag` 隔离存档确认 fullScreen=false、场景／原生窗口 Visible，切换后仍未呈现。新方案共享 PetEngine 并重建显示宿主，窗口样式和位置显式设置，设置文案实时更新。`artifacts/test-layer-v2` 真实 UI 完成悬浮→桌面→悬浮，截图确认恢复猫、猫窝、粮水盆、猫砂盆与羽毛棒；新窗口句柄且模型位置相同。83 项模型检查通过。本次不代表真实游戏／F11／多屏全屏避让已全部验收。详见[PROTOTYPE-003](../mistakes/prototype-003.md)。

2026-09-17 素材扩展追加：点击／待机各 12 个禁用空槽，支持配置启用、同基础动作随机与避免连续重复、独立帧率／循环方式；新动作边界选一次，不在绘制帧中反复抽签。83 项检查通过（含未启用／空帧／无效配置排除、组隔离、旧清单兼容、行为边界）；Release 自包含发布通过。未生成最终素材，默认画面保持现有占位表现。[资源交接](../assets/pets/bluecat/README.md)。

独立存档打包预览导出 `artifacts/variant-slots.png`，确认禁用空槽时仍正常绘制待机占位猫，未加入台词或光圈；素材清单各 12 槽及文档链接检查通过。正式存档备份为 `pet-state.json.before-variant-slots` 后更新启动桌面版。尚未用最终美术素材验收播放效果。

2026-09-17：独立持久化随机计时，喝水 10～30、吃饭 20～60、如厕 60～120 分钟；空盆／满砂持续 5 分钟后走到工作区底部中央请求。鼠标进入猫身立即带路，走一段回头等鼠标，停在对应物品旁示意操作。提前补给取消；多项缺少逐项处理。详见[最新规范](../spec/desktop-cat-care-schedules-and-guidance.md)。

- 77 项模型／存档检查通过，新增覆盖随机范围、未到期不执行、每次访问只抽签一次、离线不扣库存或补做多轮、五分钟边界、底部中央到达、立即悬停引导、等鼠标／跟随／侧方停留、搬动物品重定向、补给取消、多请求顺序、持久化，以及搬动埋砂中的猫砂盆不重复结算。
- Release win-x64 自包含发布成功。独立存档运行打包预览，`artifacts/care-request.png` 与其 JSON 确认 `request-water` 和底部中央坐标；无角色文本／判定光圈。此为应用渲染验证，不代替完整桌面层级或鼠标跟随实机验收。
- 本轮预览约 3 秒、170 帧，平均帧间隔 17.75 ms、平均绘制 0.361 ms；不据此声称完成 120 分钟真实时间浸泡验证。
- 正式存档已备份为 `pet-state.json.before-care-schedules`，测试存档未迁入正式目录。正式 `dist/Chenpi` 已更新并启动。未修改系统自启。
- 正式蓝猫序列帧、跨开机时间缺口、完整桌面／全屏／多屏与实机带路手感仍待完善。请求、带路、回头、示意和亲昵已提供独立资源动作入口，当前使用代码绘制占位动画。

## 历史增量：慢节奏、无台词和逗猫棒

双圈逗猫更新：52 项自动检查通过，覆盖外圈即时追跑、追到内圈切换抓取、内圈驻留、目标移远后反向继续追、离开外圈停止及边界判定。外圈保留原范围，内圈按真实羽毛端检测；最新资源为 `toy-run` / `toy-bat`。自包含版本编译发布并重启，最新时序已做模型验收，未据此声称完整桌面动作素材实测。

最终追加已生效：不再绘制判定光圈，内部保留猫 390／棒 170 的范围。用户操作新版预览的采样记录 4 次拎起转换，StartDrag 最大处理耗时约 0.469 ms，平均帧间隔 16.14 ms、最大 103.21 ms，平均场景绘制 0.132 ms；不是完整输入到屏幕延迟，仍需继续接收实际手感反馈。最终桌面应用重新发布并启动，测试预览退出。

最新版本已覆盖旧版几秒漫游：抵达后停留 2～60 分钟，安静 5 分钟原地睡；悬停蹭鼠标、点击互动变化和逗猫棒循环均已接入。两个判定圈半径已按用户要求扩大五倍。删除角色文本通道与场景台词，保留设置和陪伴计时。详见[最新行为规范](../spec/desktop-cat-play-and-rest.md)。

45 项自动检查通过，自包含版本重新发布。预览实测拾取棒、判定圈、交叠逗猫及再次点击归位；第一轮渲染平均间隔 16.89 ms、平均画面绘制 0.215 ms。用户进一步反馈卡顿集中于长按判定瞬间后，追加姿态预热、250 ms 阈值、按住移动直接拾取、平滑抬升及连续呼吸／尾巴动画。新版已启动交互预览；UI 工具检测用户正在操作，停止继续输入，不把早期整体帧率测量冒充最新长按手感实测。

以下为前一版的基础功能与已测边界记录；以上最新行为覆盖旧版相应描述。

## 本次可体验版本

- Windows x64，C# / WPF / .NET 10；工作区 SDK `.tools/dotnet`，自包含应用 `dist/Chenpi/Chenpi.exe`，约 171 MiB。无需另装 .NET；开发体验包尚未优化体积。
- 默认桌面层级，右下角紧凑五物件，缩放 0.85。设置可切换悬浮、静音、音量、缩放、自启；未替用户开启开机自启。
- 无饥渴、库存或猫砂百分比；粮粒、水面大小与波纹、猫砂结块表示状态，补给平滑变化。保留猫窝陪伴时间。
- 饭盆、水盆、猫砂盆、猫窝独立拖动。短按释放才补给／清理；物件移动超过 6 个逻辑单位进入拖动，长按也可进入。猫长按约 0.42 秒拾起，释放到猫窝睡觉。
- 位置自动保存，缩放／尺寸变化按场景比例恢复并约束边界。重置布局保留陪伴时间与照料状态。睡猫随猫窝移动；搬动正在使用的盆中断旧动作，重新前往目标。
- Selector / Sequence 条件行为树及动作执行器：走动、吃喝、睡觉、如厕／埋砂、催促、互动、卖萌。仅实际摄入扣除粮水。
- 蓝灰色代码绘制占位猫尚不是最终序列帧。`assets/pets/bluecat/manifest.json` 提供逐动作 PNG 接入，空动作回退占位图。

## 已执行验证

- Release 编译、win-x64 自包含发布成功。
- 26 项逻辑／存档检查：计时增需求但不扣盆、实际摄入、缺粮仍可互动、拖拽中断扣粮、入窝与唤醒、提醒冷却、新旧开机、紧凑布局、物件移动及重寻路、睡猫随窝、边界、缩放、重置不影响照料、保存／读取及备份恢复。
- computer-use 在真实 WPF 交互预览窗口分别拖动四件家具；粮食、水、猫砂值未因拖动变化。单独点击水盆补满，位置不变；余量仅画面显示。
- 正常退出，再用打包 exe 加载同一测试存档；四件家具坐标全部保留，库存未被退出补算扣除。
- 设置窗口无照料百分比；食水盆和猫砂盆旁无常驻操作标签。
- 默认桌面启动诊断 `attached=true`，挂接桌面宿主，右下角初始坐标正确。`artifacts/desktop-v2.png` 是应用自身渲染，**不是完整桌面层级实测截图**。
- 预览及测试使用 `artifacts/test-*` 隔离存档，正式应用使用 `%LOCALAPPDATA%/Chenpi`。

## 未完成／未充分实测

- 跨重启完整有效运行历史。同次开机可补算退出间隔，Windows unbiased interrupt time 排除睡眠／休眠；新开机发现历史缺口保留进度，不用自然时间冒充。未安装后台服务／计划任务。
- Win+D、Explorer 重启、真实游戏／视频／F11 全屏、混合 DPI、多屏切换、自启的完整实测。已有挂接、全屏避让和显示变更代码，不把这些全部标成通过。
- 猫长按拖放及窝内点击在原生桌面上的完整人工验收。行为逻辑已测；工具的快速拖动验证家具，没有据此声称验证猫的长按。
- 最终蓝猫序列帧、真实叫声、节日皮肤。现有声音是默认静音的合成占位音。
- 性能指标和多屏自由移动需后续验证，当前仅主屏场景。

## 复现

```powershell
.tools/dotnet/dotnet.exe run --project tests/Chenpi.Tests/Chenpi.Tests.csproj -c Release
.tools/dotnet/dotnet.exe publish src/Chenpi/Chenpi.csproj -c Release -r win-x64 --self-contained true -o dist/Chenpi
```

测试支持 `--preview --data-dir <隔离目录>`；`--snapshot <路径> --exit-after-snapshot` 初始化后导出应用渲染和挂接诊断并退出。不要把测试存档迁入正式目录。
