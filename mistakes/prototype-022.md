# PROTOTYPE-022：循环停留时间不是可直接退出的帧

扶墙默认停留5秒，但53/56循环不是5秒整。直接计时退出会在循环中间接54/57，实测左56第23帧到57第0帧触发宽高与位移警报。修复让停留时间成为最早退出时间，播放器按当前循环起点计算其后的完整周期边界；重复手动触发同一姿态也应使用新的最早退出时间。不得用改体型、改速或叠帧掩盖。

检查必须运行真实行为路由，静态末帧→首帧接缝不能发现运行时截断。左右自主/手动路径、完整循环和输入中断都要覆盖。素材固有变化单列，不把逻辑检查通过当成美术全部通过。
