# PROTOTYPE-020：行为参数编辑不能停在界面与JSON

2026-09-20 接入行为设置时，现有构造器仍把大于3600秒的休息周期视为无效；新工具允许最长240分钟，这会导致合法长周期重启后被重新抽取。已把存档合法范围与新参数范围对齐，并验证7200秒周期和已用时在重启/应用参数后保留。

概率字段须使用真实决策读取，不能只保存配置或修改图上文案。新增测试通过只保留掩面权重、只保留侧躺叫声、修改点击/悬停窗口、关闭走路叫，实际推进模型和动画采样确认生效。普通保存保留照料期限；显式重排单独验证库存不变。

界面截图首版直接渲染Content，漏掉Window提供的底色，导出背景透明；正式窗口本身不是黑底。已让根面板显式持有底色，再渲染并检查，不把截图错误误诊成桌宠渲染问题。窗口自动化夹具与人工鼠标验证保持区分。
