# PROTOTYPE-017：重描窝沿不能修正错误的前后层

用户再次反馈后腿和尾根被侧沿切断。此前只重描遮挡线，并在预览里把高侧沿截断猫后半身误认为正常遮挡；根节点进入坐垫时，整个侧沿一并置前，遗漏了完整身体的进入进度。

本次高侧沿保留后景，只将低前沿放在猫前面。禁止把变换遮罩形状当成层级关系已解决。增加实际WPF场景像素审计：比较猫alpha与前景alpha，检查脚底以上身体覆盖量，以旧错误遮罩为负对照。单猫逐帧尺寸工具不能替代场景遮挡检查；须检查完整进出和用户报告的具体部位。见[窝前景层级](../docs/nest-foreground-depth.md)。
