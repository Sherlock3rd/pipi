# PROTOTYPE-023 扶墙应与普通移动共用目的地池

错误：把屏边扶墙挂在入睡判断下，并限制已在屏边40单位内、每个进程只执行一次，导致从普通位置不能自主选择走向墙边。

纠正：以用户确认的优先级为准。建立清醒移动选择器，普通安全地点和扶墙共用权重；先排除不可用目标。左右墙均分扶墙总权重，不因两个侧边翻倍。去墙边也完整播放移动链；照料和输入优先。

验证：从远处自主走向左右墙、重复访问、阻挡过滤、途中物品搬动、全零权重、睡眠不被叫醒和照料抢占都须测。必须核对真实运行分支，不能只改流程图。
