# Sleep FULL30 独立静态复审

日期：2026-09-18。本次对象为 `decoded/sleep-full.png` 提取的 `frames/sleep/00.png`～`29.png`，不是被否决的十帧 pilot。下文帧号均从 1 开始；帧 25 对应 `24.png`。

**结论：PASS，限于本批静态身份、解剖、睡姿语义与浅／深底透明呈现。完整动画验收仍为 PENDING，须由实际播放检查闭合。** 本报告未观看实时动画，因此不宣称呼吸节奏、正常／半速播放及循环动态已经通过，也不据此授权单独替换运行时。

## 检查依据与范围

- 按项目 `acceptance.md`、`qa/identity-02-review.md` 和 hatch-pet 的身份／结构／动态分离要求检查。
- 直接对照 `references/canonical-sheet.png` 的右下蜷睡猫与原始 `cat-reference-05.jpg`。检查浅底 `sleep-contact.png`、深底 `sleep-contact-dark.png` 的全部 30 帧，另单独查看帧 1、21、25、26、30。
- 读取 `sleep-source.json`，独立读取全部最终 PNG 的实际模式、尺寸、半透明主体边界和 SHA256。结果绑定在 `sleep-30-review.json`。源文件 SHA256：`f1f5cb4c808117c037ab0878eb115995a27220443f3097130436fe4f5b09065a`。
- 只检查并写报告，未修改帧、manifest、生成源图或播放参数。低 alpha 清理后的当前文件是本报告对象；它不能被解释为重新绘制猫体。

## 逐帧观察

1. **帧 1～5：身份与基本睡姿通过。** 圆颊、短鼻口、灰色口鼻、双耳、厚颈与蓝灰厚涂保持标准图身份。闭眼且脸朝内，头有前爪附近的枕点；肩颈接入背部连续。没有为露出四足而画出穿身的远肢。照片的侧向 C 形收团、背臀承重、头内收得到保留；视角比照片多露侧脸与标准图一致，不视为身份失败。
2. **帧 6～14：头、前爪和尾的遮挡关系通过。** 近侧前爪从胸颈内侧伸向脸前，另一前爪在鼻下附近部分遮挡，后腿收在臀腹体积中。外侧粗尾从右后躯绕向前侧，保持单尾与圆钝尾端；没有额外爪、尾变腿、头颈脱节或爪长在腹部的明确迹象。细毛块有所重绘，但联系表上没有变白嘴垫或新花纹。
3. **帧 15～20：体型保持。** 腰背、头颅与后躯没有明显的大小替换或换面。鼻口依旧靠近前爪，臀部与尾的支撑轮廓没有突然消失。这里不能仅凭帧内容不同推定存在有效呼吸。
4. **帧 21～25：局部最高／最宽阶段，静态可接受。** 圆背略上抬，帧 23 是半透明主体面积最大帧；仍是同一只成年蓝猫，未见 pilot 的明显充气。头、爪、尾保持原本可解释的连接，未出现肉球化或摊开四足。
5. **帧 25→26：必须动态重点复核。** 主体顶端从 y=107 降到 y=110（alpha≥128），包含羽化边缘的源统计为 y=104→108；主体质心 y=172.435→174.278，单步下移 1.843 px。帧 26 的睡姿本身正常，但这次收缩是否在 15 fps 中读成跳变不能由静态并排证实。按 256 画布缩到 180 画布显示，实心顶端为约 2.11 px、质心为约 1.30 px；若以猫主体宽 180 px 显示，位移接近原测量。必须使用真实预览尺寸，不能混淆这两种“180 px”。
6. **帧 26～30及 30→1：静态接点通过。** 头朝向、前爪枕点、单尾包身和灰色鼻口都延续。帧 30 与帧 1 的 alpha≥128 顶部／底部均为 y=108／235，主体质心差 x=-0.438、y=+0.146 px；接点没有姿态重置、突然睁眼、换脸或肢体增减。首帧主体实心宽 177 px，末帧 180 px，小幅轮廓差仍须循环播放确认，不能直接叫作无缝。

## 量化佐证及其边界

30 张文件均为 256×256 RGBA，哈希与 `sleep-source.json` 当前记录一致。alpha≥128 的主体宽度为 177～182 px，高度为 125～129 px；实心面积为 16,947～17,607 px，最大／最小约 1.039。主体质心 x 跨度 1.322 px、y 跨度 2.399 px。包含柔边的下边界在源记录中统一为 236，半透明阈值下为 235／236。这些值支持“没有旧 pilot 的 13% 明显体积膨胀”，但面积不是解剖体积，质心也不是头骨或尾根定位；本报告没有把像素统计冒充自动骨骼追踪。

浅／深底联系表上未见背景矩形、落地投影残留、悬浮碎片或明显色晕。爪、脸与尾部的小笔触差异存在，但静态实际尺度没有构成可见解剖或身份破坏。这里不采用“任一像素变化即失败”的规则，也不把 30 个不同哈希当作呼吸完成的证据。

## 剩余放行门槛

**无静态强制重绘项。** 动态放行仍须完成：正常及半速正序检查，尤其 25→26、29→30→1；确认读到缓慢胸腹呼吸而非随机全身纹理闪动、突然回落或整体缩放。随后还需运行时地面／猫窝遮挡及点击、拖拽中断验证。本次未检查入睡过渡与该睡姿的端点衔接。若动态发现明显跳变，应只修复被定位的段落，不能用本静态 PASS 覆盖失败。

本报告属于独立内部复审，不等于用户最终定稿，也不撤销旧 24 帧和十帧 pilot 的失败结论。
