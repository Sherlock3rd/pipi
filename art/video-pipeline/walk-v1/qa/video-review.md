# 用户右移动视频接入预览（2026-09-18）

- 用户要求将新视频接入桌宠，启动查看右移动效果。
- 原视频：`incoming/right-walk.mp4`，与用户下载目录原件 SHA256 均为 `c20732cb9d808b436efbd4e3cef0510028171aad60825dfea518fab50c790077`；1280×720，24 fps，121 帧。
- 选取源帧 62（含）至 101（不含），1.625 秒。按时间抽取 30 张不同源画面，播放约 18.461538 fps；未复制、倒放或 AI 补帧。选择依据是相近步态相位和像素差，不能单凭像素差认定步态通过。
- `tools/extract_right_walk_video.py` 使用前景种子、GrabCut 分割及边缘收缩；全段共同裁切、比例、画布和基线，透明区 RGB 归零。已查看深底全帧图及 WPF 浅底实绘图；初版腿间背景残留已处理。细胡须可能损失，动态脚掌遮挡与循环自然度仍待用户评审。
- 仅新增 `walk-right` 并按朝向选择，左行继续旧素材；右行结束仍走原坐下过渡。不是整套美术通过。
- 新增 `--preview --preview-walk` 隔离预览：小猫持续向右移动并从左侧重新进入，用独立存档；普通启动仍是完整桌宠。预览没有把画面位置同步到行为模型，专用于看素材，不用于交互验收。
- .NET SDK 10.0.401 本地安装，压缩包 SHA512 与微软发布元数据一致。127 项逻辑检查通过；自包含发布成功。WPF 运行快照显示 `DisplayedClip=walk-right`、`DisplayedFrame=27`，进程响应正常；静态快照不等于动态验收。
- 本地快捷方式：根目录“查看右移动.lnk”与“启动陈皮.lnk”；分发入口 `dist/Chenpi/Chenpi.exe`。预览已启动，正式个人存档未使用。未提交／推送。
