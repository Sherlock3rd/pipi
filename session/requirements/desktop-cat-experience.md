# 需求：桌面养猫体验

- 日期：2026-09-17。
- 状态：Windows 可体验原型已实现、打包并运行；部分完整需求仍待完善，见[原型验证](../../docs/prototype-validation.md)。
- 用户明确：蓝猫、五物件、行为树、喂饭喂水铲屎、点击／长按交互、睡眠唤醒、系统作息／计时、自启／层级设置、独立序列帧与后续节日皮肤。
- 单一需求来源：[desktop-cat-brief](../../spec/desktop-cat-brief.md)。

## 变更与继承

最新：用户授权继续开发，推进显示稳定性。全屏判断改用猫所在屏幕，读取可见边界并排除最小化、隐藏和 cloaked 窗口；普通最大化不避让，副屏全屏不误隐藏主屏猫。110 项逻辑检查和两屏环境 12 项原生窗口检查通过；完整前台游戏／视频／F11 与混合 DPI 切换仍待验收。详见[验证记录](../../docs/prototype-validation.md)与[PROTOTYPE-005](../../mistakes/prototype-005.md)。

最新：用户反馈调试动作和放回窝睡眠失效。复现了旧 CareRequest 覆盖手动指令，以及照料调度绕过睡眠保护；另修复头部拖入可见猫窝但脚锚点不命中。现在手动动作完整执行后归还控制，初始 15 秒睡眠安定期可点醒，缺货计时不重置，空盆不自动补给。97 项检查通过，预览诊断确认窝内睡眠；完整实机按钮／拖放体验待用户反馈，见[错题](../../mistakes/prototype-004.md)。

最新：用户指定项目内容提交到 [Sherlock3rd/pipi](https://github.com/Sherlock3rd/pipi)。首批源码、83 项检查、资源配置和 specrule 文档已通过 `64b08f3` 提交并推送至 `main`，完整远端哈希已核对；构建产物与个人运行状态排除。后续交付沿用此仓库，提交结果见总控会话。

最新：用户反馈的“全屏功能”已明确为小猫设置里的悬浮开关。旧透明窗口运行中换父级后画面不恢复，fullScreen=false，排除本次由全屏检测主动隐藏。现改为共享原模型并重建显示宿主，保留物品位置、照料期限和行为；设置说明更新。真实往返切换已恢复显示，见[窗口错题](../../mistakes/prototype-003.md)。

最新：用户要求预留点击互动和待机表现类型数量，待美术风格确定后批量添加。实现默认各 12 槽、数量可扩展，全部默认禁用；清单提供动作创意草案。渲染层读取配置，已启用且有完整帧的变体才参与随机，避免同一基础动作连续重复；旧素材和行为逻辑兼容。83 项检查与自包含发布通过。[资源说明](../../assets/pets/bluecat/README.md)含批量接入步骤。

最新：用户指定随机喝水 10～30、吃饭 20～60、如厕 60～120 分钟；空盆／满猫砂持续 5 分钟后底部中央求助，鼠标进入猫身即覆盖为带路。已实现独立持久化期限、请求轮换、回头等待、终点侧方示意、搬动物品重定向、提前补给取消；无文字／光圈／惩罚。77 项检查通过，见[随机照料规范](../../spec/desktop-cat-care-schedules-and-guidance.md)。此前交付记录如下。

最新：用户要求猫双判定圈与两种逗猫行为。保留外圈范围，检测后立即朝目标跑；新增贴身内圈，进入后停下抓棒。实时切换、放下／外圈外停止，取消原先等待观察和短扑循环。52 项模型／存档检查通过，Windows 自包含发布并重启；规则与资源清单同步，内圈半径 100、追速 240 为实现默认。

最终澄清：判定光圈不用显示，删除范围覆盖层但保留五倍半径逻辑。新版已打包并重启正式桌宠。用户预览操作的诊断得到 4 次拎起转换、处理最大 0.469 ms；不冒充完整输入延迟。独立测试存档与正式进度分开。

最新新增：用户要求优化拎起判定瞬间卡顿、悬停 5 秒蹭鼠标、移动抵达抽取 2～60 分钟停留、安静 5 分钟原地睡眠、点击唤醒后概率换位／互动，以及窝旁逗猫棒拾取与圈相交玩法。进一步要求圈半径扩大五倍、全程角色无文本台词，均已写入规则并实现。45 项检查通过，交互预览完成逗猫拾取／交叠／放下验证；用户正操作预览，自动输入停止。最新逻辑、输入优化与验证限制见[休息与玩耍](../../spec/desktop-cat-play-and-rest.md)。

最新：用户已明确授权开工及重试；采用 C# / WPF / .NET 10，启动 `dist/Chenpi/Chenpi.exe`。去除具体照料数值，以粮粒、水面／波纹、猫砂结块表示状态，补给平滑变化；默认紧凑布局。四件家具可自由拖动、自动保存，拖动不触发补给；小猫重新前往被移动的目标，睡猫随窝搬动。26 项逻辑／存档检查通过；UI 四件家具拖动、单击加水、退出再启动后位置恢复通过。正式猫序列帧、跨重启有效时间、完整桌面／全屏／多屏仍待完成。

以下为历史规划记录，不能覆盖上述最新授权和实现状态。

同步 AGENTS、通用规则、业务边界、总控会话、术语及调研方向；BongoCat 不再作为核心体验推荐。增加 VPet、DyberPet、SondeR-Cat、deskcat 的匹配与成熟度比较。

本轮用户已明确入窝睡觉；框架和具体照料数值仍未定，没有产品实现。后续严格区分确认需求与建议。

## 本轮规划交付

- 新增[实现计划](../../spec/desktop-cat-implementation-plan.md)：7 阶段、逐阶段验收、桌面能力与计时风险、行为树组织、序列帧列表、补充建议。
- 三个异步问题均已获用户答复：电脑开着即使桌宠完全退出也消耗，睡眠／休眠暂停；仅游戏／视频／F11 全屏隐藏，普通最大化可悬浮；入窝支持点击立即唤醒与作息／需求自然醒。
- 计时记录独立于桌宠可视程序，跨重启方案尚未决定，未安装后台服务或自启任务。
- 用户已说明特殊日期皮肤后做；计划预留资源锚点，未提前生成节日素材。
- 本轮检查文档引用与已确认规则一致性，不声称桌面／计时能力已经实现。

## 遗漏项补全与最终照料口径

- 用户授权补齐上一轮遗漏内容，并明确“没有不可逆惩罚，只有催促”“运行时间增加饥饿和口渴，小猫实际吃喝时扣盆里的量”。
- 新增[照料与交互补充规范](../../spec/desktop-cat-care-and-interaction.md)，将此前建议落实为正式需求与可调整默认值：三类时间分离、自然陪伴时长、释放入窝、提醒冷却／合并／静音、托盘召回与布局恢复、缩放、存档恢复、屏幕适配及过渡序列帧。
- 时间只增加需求，程序退出后重开仅补增饥渴；库存不随时间自动减少，不回放离线吃喝。补盆不直接解除猫的饥渴，需要实际吃喝。
- 未照料只有催促，不能添加死亡、离家、关系／属性惩罚或操作锁定；动作被打断不能重复扣库存。
- 已同步 AGENTS、基础／业务规则、简述、实现计划、总控会话及术语，检查链接与冲突文字；仅文档变更，未开始产品实现或安装后台。

2026-09-17 显示恢复：反馈看不到时进程未运行，无错误日志。已重启并将当前设置切到悬浮显示，存档保留；退出原因未确认。
